<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= fileName %> - QuickShare</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
    .glass {
      background: rgba(255, 255, 255, 0.12);
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      border-radius: 1rem;
      border: 1px solid rgba(255, 255, 255, 0.2);
    }
  </style>
</head>
<body class="bg-gradient-to-br from-gray-900 via-slate-800 to-gray-900 min-h-screen flex items-center justify-center px-4">

  <div class="glass shadow-2xl max-w-xl w-full p-8 text-center text-white">
    <h1 class="text-3xl font-extrabold text-yellow-300 mb-2">📦 Shared File</h1>
    <p class="text-lg font-semibold text-white mb-6 break-words"><%= fileName %> (<%= fileSize %>)</p>

    <% if (fileName.match(/\.(jpg|jpeg|png|gif|webp)$/i)) { %>
      <img src="<%= cloudLink %>" alt="preview" class="mx-auto max-h-64 mb-4 rounded-xl shadow-md">
    <% } else if (fileName.match(/\.(mp4|webm|ogg)$/i)) { %>
      <video controls class="mx-auto max-h-64 mb-4 rounded-xl">
        <source src="<%= cloudLink %>" type="video/mp4">
        Your browser does not support the video tag.
      </video>
    <% } else { %>
      <div class="mb-4 px-4 py-2 bg-white/10 rounded-lg text-sm text-gray-300">Preview not available for this file type.</div>
    <% } %>

    <a href="<%= downloadLink %>" download class="inline-block px-6 py-3 bg-emerald-500 text-white font-bold rounded-lg hover:bg-emerald-600 transition">⬇️ Download</a>

    <footer class="mt-6 text-xs text-gray-300">
      Shared securely via <span class="font-bold text-yellow-300">QuickShare</span> 🚀<br>
      Made by Somesh 💛
    </footer>
  </div>

</body>
</html>
