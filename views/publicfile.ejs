<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Download <%= fileName %> | Quickshare</title>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    body {
      font-family: 'Outfit', sans-serif;
      background-color: #030712;
      color: white;
    }

    .glass {
      background: rgba(17, 24, 39, 0.7);
      backdrop-filter: blur(20px);
      -webkit-backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .gradient-text {
      background: linear-gradient(to bottom, #ffffff, #a5b4fc);
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
    }
  </style>
</head>

<body class="min-h-screen flex flex-col items-center justify-center p-4 relative overflow-hidden">

  <!-- Background Gradients -->
  <div class="fixed top-0 left-0 w-full h-full overflow-hidden -z-10 pointer-events-none">
    <div
      class="absolute top-[-10%] left-[-10%] w-[500px] h-[500px] bg-blue-600/20 rounded-full blur-[100px] animate-pulse">
    </div>
    <div
      class="absolute bottom-[-10%] right-[-10%] w-[500px] h-[500px] bg-purple-600/20 rounded-full blur-[100px] animate-pulse"
      style="animation-delay: 2s;"></div>
  </div>

  <!-- Main Card -->
  <div class="glass w-full max-w-md rounded-3xl p-8 shadow-2xl relative z-10 text-center">

    <!-- Logo -->
    <div class="mb-8">
      <h1 class="text-3xl font-bold tracking-tight gradient-text">Quickshare</h1>
      <p class="text-gray-400 text-sm mt-1">Ready for download</p>
    </div>

    <!-- File Preview Section -->
    <div class="mb-8 relative group">
      <div
        class="w-full h-48 bg-gray-900/50 rounded-2xl border border-white/10 flex items-center justify-center overflow-hidden relative">

        <% if (cloudLink.match(/\.(jpg|jpeg|png|gif|webp)$/i)) { %>
          <!-- Image Preview -->
          <img src="<%= cloudLink %>" alt="Preview" class="w-full h-full object-contain p-2">
          <% } else if (cloudLink.match(/\.(mp4|webm|mov)$/i)) { %>
            <!-- Video Preview -->
            <video src="<%= cloudLink %>" controls class="w-full h-full object-contain"></video>
            <% } else if (cloudLink.match(/\.pdf$/i)) { %>
              <!-- PDF Icon -->
              <svg class="w-16 h-16 text-red-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M7 21h10a2 2 0 002-2V9.414a1 1 0 00-.293-.707l-5.414-5.414A1 1 0 0012.586 3H7a2 2 0 00-2 2v14a2 2 0 002 2z">
                </path>
              </svg>
              <% } else { %>
                <!-- Generic File Icon -->
                <svg class="w-16 h-16 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z">
                  </path>
                </svg>
                <% } %>

      </div>

      <div class="mt-4">
        <h2 class="text-lg font-semibold text-white truncate px-4" title="<%= fileName %>">
          <%= fileName %>
        </h2>
        <div class="flex items-center justify-center gap-2 mt-1">
          <span class="px-2 py-0.5 rounded-md bg-white/10 text-xs font-mono text-gray-300">
            <%= fileSize %>
          </span>
          <% if (Number(fileSize.split(' ')[0]) > 20) { %>
                        <span class="text-xs text-yellow-500/80">Large File</span>
                    <% } %>
                </div>
            </div>
        </div>

        <!-- Download Button -->
        <a href="<%= downloadLink %>" download class="block w-full py-4 rounded-xl bg-white text-black font-bold text-lg hover:scale-[1.02] active:scale-[0.98] transition-transform shadow-xl shadow-white/10 flex items-center justify-center gap-2">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path></svg>
            Download Now
        </a>

        <!-- Footer -->
        <p class="text-xs text-gray-500 mt-6">
            Link expires in 24 hours. <br>
            <a href="" class="text-blue-400 hover:text-blue-300 transition-colors">Create your own link</a>
        </p>

    </div>

</body>
</html>